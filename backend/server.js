import express from 'express';import cors from 'cors';import './db.js';
import applications from './routes/applications.js';import stats from './routes/stats.js';import reminders from './routes/reminders.js';import interviews from './routes/interviews.js';import companies from './routes/companies.js';import ai from './routes/ai.js';
const app=express();app.use(cors());app.use(express.json({limit:'2mb'}));app.get('/api/health',(q,s)=>s.json({ok:true}));app.use('/api/applications',applications);app.use('/api/stats',stats);app.use('/api/reminders',reminders);app.use('/api/interviews',interviews);app.use('/api/companies',companies);app.use('/api/ai',ai);app.use((e,q,s,n)=>{console.error(e);s.status(500).json({error:'Internal server error'});});app.listen(process.env.PORT||4000,()=>console.log('API running at http://localhost:4000'));
